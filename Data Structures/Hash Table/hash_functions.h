#pragma once
#define TABLE_SIZE 13

// Division Method
size_t division_hash(size_t);

// Multiplication Method
size_t multiply_hash(size_t);

// Universal Hash Function by Carter and Wegman
size_t universal_hash(size_t);